
<isset name="productID" value="${pdict.productId}" scope="page"/>
<p>The product: ${productID}</p>

<isdecorate template="common/layout/page">
<isinclude url="${URLUtils.url('Tile-Show', 'pid', pdict.productId, 'swatches', true, 'ratings', true)}" />
</isdecorate>